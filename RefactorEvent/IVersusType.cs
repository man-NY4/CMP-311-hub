using System;

namespace RefactorEvent
{
    public interface IVersusType // interface that will print whether a tournament is one on one vs or team vs
    {
        void printVersusType(string message);
    }
}